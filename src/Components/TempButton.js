import React from 'react'
import './tempCommon.css'

const TempButton = (props) => {
  return (
    <div>
        <button className='button-39'>{props.title}</button>
    </div>
  )
}

export default TempButton;